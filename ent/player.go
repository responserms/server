// Code generated by entc, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"github.com/facebook/ent/dialect/sql"
	"github.com/responserms/server/ent/metadata"
	"github.com/responserms/server/ent/player"
	"github.com/responserms/server/ent/user"
)

// Player is the model entity for the Player schema.
type Player struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// Name holds the value of the "name" field.
	Name string `json:"name,omitempty"`
	// TotalMinutes holds the value of the "total_minutes" field.
	TotalMinutes int `json:"total_minutes,omitempty"`
	// SessionStartedAt holds the value of the "session_started_at" field.
	SessionStartedAt *time.Time `json:"session_started_at,omitempty"`
	// SessionEndedAt holds the value of the "session_ended_at" field.
	SessionEndedAt *time.Time `json:"session_ended_at,omitempty"`
	// LastSeenAt holds the value of the "last_seen_at" field.
	LastSeenAt *time.Time `json:"last_seen_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the PlayerQuery when eager-loading is set.
	Edges           PlayerEdges `json:"edges"`
	player_metadata *int
	player_user     *int
}

// PlayerEdges holds the relations/edges for other nodes in the graph.
type PlayerEdges struct {
	// Metadata holds the value of the metadata edge.
	Metadata *Metadata
	// Servers holds the value of the servers edge.
	Servers []*Server
	// User holds the value of the user edge.
	User *User
	// Identifiers holds the value of the identifiers edge.
	Identifiers []*PlayerIdentifier
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [4]bool
}

// MetadataOrErr returns the Metadata value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e PlayerEdges) MetadataOrErr() (*Metadata, error) {
	if e.loadedTypes[0] {
		if e.Metadata == nil {
			// The edge metadata was loaded in eager-loading,
			// but was not found.
			return nil, &NotFoundError{label: metadata.Label}
		}
		return e.Metadata, nil
	}
	return nil, &NotLoadedError{edge: "metadata"}
}

// ServersOrErr returns the Servers value or an error if the edge
// was not loaded in eager-loading.
func (e PlayerEdges) ServersOrErr() ([]*Server, error) {
	if e.loadedTypes[1] {
		return e.Servers, nil
	}
	return nil, &NotLoadedError{edge: "servers"}
}

// UserOrErr returns the User value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e PlayerEdges) UserOrErr() (*User, error) {
	if e.loadedTypes[2] {
		if e.User == nil {
			// The edge user was loaded in eager-loading,
			// but was not found.
			return nil, &NotFoundError{label: user.Label}
		}
		return e.User, nil
	}
	return nil, &NotLoadedError{edge: "user"}
}

// IdentifiersOrErr returns the Identifiers value or an error if the edge
// was not loaded in eager-loading.
func (e PlayerEdges) IdentifiersOrErr() ([]*PlayerIdentifier, error) {
	if e.loadedTypes[3] {
		return e.Identifiers, nil
	}
	return nil, &NotLoadedError{edge: "identifiers"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Player) scanValues(columns []string) ([]interface{}, error) {
	values := make([]interface{}, len(columns))
	for i := range columns {
		switch columns[i] {
		case player.FieldID, player.FieldTotalMinutes:
			values[i] = &sql.NullInt64{}
		case player.FieldName:
			values[i] = &sql.NullString{}
		case player.FieldCreatedAt, player.FieldUpdatedAt, player.FieldSessionStartedAt, player.FieldSessionEndedAt, player.FieldLastSeenAt:
			values[i] = &sql.NullTime{}
		case player.ForeignKeys[0]: // player_metadata
			values[i] = &sql.NullInt64{}
		case player.ForeignKeys[1]: // player_user
			values[i] = &sql.NullInt64{}
		default:
			return nil, fmt.Errorf("unexpected column %q for type Player", columns[i])
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Player fields.
func (pl *Player) assignValues(columns []string, values []interface{}) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case player.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			pl.ID = int(value.Int64)
		case player.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				pl.CreatedAt = value.Time
			}
		case player.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				pl.UpdatedAt = value.Time
			}
		case player.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				pl.Name = value.String
			}
		case player.FieldTotalMinutes:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field total_minutes", values[i])
			} else if value.Valid {
				pl.TotalMinutes = int(value.Int64)
			}
		case player.FieldSessionStartedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field session_started_at", values[i])
			} else if value.Valid {
				pl.SessionStartedAt = new(time.Time)
				*pl.SessionStartedAt = value.Time
			}
		case player.FieldSessionEndedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field session_ended_at", values[i])
			} else if value.Valid {
				pl.SessionEndedAt = new(time.Time)
				*pl.SessionEndedAt = value.Time
			}
		case player.FieldLastSeenAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field last_seen_at", values[i])
			} else if value.Valid {
				pl.LastSeenAt = new(time.Time)
				*pl.LastSeenAt = value.Time
			}
		case player.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field player_metadata", value)
			} else if value.Valid {
				pl.player_metadata = new(int)
				*pl.player_metadata = int(value.Int64)
			}
		case player.ForeignKeys[1]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field player_user", value)
			} else if value.Valid {
				pl.player_user = new(int)
				*pl.player_user = int(value.Int64)
			}
		}
	}
	return nil
}

// QueryMetadata queries the metadata edge of the Player.
func (pl *Player) QueryMetadata() *MetadataQuery {
	return (&PlayerClient{config: pl.config}).QueryMetadata(pl)
}

// QueryServers queries the servers edge of the Player.
func (pl *Player) QueryServers() *ServerQuery {
	return (&PlayerClient{config: pl.config}).QueryServers(pl)
}

// QueryUser queries the user edge of the Player.
func (pl *Player) QueryUser() *UserQuery {
	return (&PlayerClient{config: pl.config}).QueryUser(pl)
}

// QueryIdentifiers queries the identifiers edge of the Player.
func (pl *Player) QueryIdentifiers() *PlayerIdentifierQuery {
	return (&PlayerClient{config: pl.config}).QueryIdentifiers(pl)
}

// Update returns a builder for updating this Player.
// Note that, you need to call Player.Unwrap() before calling this method, if this Player
// was returned from a transaction, and the transaction was committed or rolled back.
func (pl *Player) Update() *PlayerUpdateOne {
	return (&PlayerClient{config: pl.config}).UpdateOne(pl)
}

// Unwrap unwraps the entity that was returned from a transaction after it was closed,
// so that all next queries will be executed through the driver which created the transaction.
func (pl *Player) Unwrap() *Player {
	tx, ok := pl.config.driver.(*txDriver)
	if !ok {
		panic("ent: Player is not a transactional entity")
	}
	pl.config.driver = tx.drv
	return pl
}

// String implements the fmt.Stringer.
func (pl *Player) String() string {
	var builder strings.Builder
	builder.WriteString("Player(")
	builder.WriteString(fmt.Sprintf("id=%v", pl.ID))
	builder.WriteString(", created_at=")
	builder.WriteString(pl.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", updated_at=")
	builder.WriteString(pl.UpdatedAt.Format(time.ANSIC))
	builder.WriteString(", name=")
	builder.WriteString(pl.Name)
	builder.WriteString(", total_minutes=")
	builder.WriteString(fmt.Sprintf("%v", pl.TotalMinutes))
	if v := pl.SessionStartedAt; v != nil {
		builder.WriteString(", session_started_at=")
		builder.WriteString(v.Format(time.ANSIC))
	}
	if v := pl.SessionEndedAt; v != nil {
		builder.WriteString(", session_ended_at=")
		builder.WriteString(v.Format(time.ANSIC))
	}
	if v := pl.LastSeenAt; v != nil {
		builder.WriteString(", last_seen_at=")
		builder.WriteString(v.Format(time.ANSIC))
	}
	builder.WriteByte(')')
	return builder.String()
}

// Players is a parsable slice of Player.
type Players []*Player

func (pl Players) config(cfg config) {
	for _i := range pl {
		pl[_i].config = cfg
	}
}
